<%= form_for(@type_consult) do |f| %>
  <% if @type_consult.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_consult.errors.count, "error") %> prohibited this type_consult from being saved:</h2>

      <ul>
      <% @type_consult.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  
<div>
  <div>Nombre:</div>
  <%= f.text_field :name %>
</div>

<div>
  <div>Descripcion:</div>
  <%= f.text_area :description, :cols => 20, :rows => 10 %>
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
